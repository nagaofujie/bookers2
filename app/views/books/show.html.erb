<p id="notice"></p>
    <div class="container px-5 px-sm-0">
    <div class="row">
        <%= render 'books/errors.html.erb',book: @book %>
        <%= render 'books/book.html.erb',book: @books,user: @user %>
        <div class="col-md-8 offset-md-1">
        <h2>Book detaill</h2>
    <table class="table">
  		  <tbody>
  		    <tr>
          <td><%= image_tag @user.get_profile_image("100x100") %><br>
          <%= link_to @user.name,user_path(@user.id) %>
          </td>
          <td><%= link_to @book.title,book_path(@book.id) %></td>
          <td><%= @book.body %></td>
          <td> <% if @book.user==current_user %>
            <%=link_to "Edit",edit_book_path(@book.id) ,class:"btn btn-sm btn-success" %>
            <% end %>
          </td>
          <td><% if @book.user==current_user %>
          <%= link_to "Destroy",book_path(@book.id),method: :delete ,class:"btn btn-sm btn-danger " %>
            <% end %>
          </td>
          </tr>
        </tbody>
     </table>
    </div>
    </div>
    </div>


