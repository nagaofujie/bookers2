  <p id="notice"></p>
    <div class="container px-5 px-sm-0">
      <div class="row">
      <%= render 'books/errors.html.erb',book: @book %>
      <%= render 'books/book.html.erb',book: @book,user: @user %>
        <div class="col-md-8 offset-md-1">
          <h2>Books</h2>
            <table class="table">
              <thead>
                <tr>
                  <th></th>
                  <th>Title</th>
                  <th>Opinion</th>
                </tr>
              </thead>
      <tbody>
        <% @books.each do |book| %>
          <tr>
            <%#= link_to image_tag(book.user.get_profile_image('80x80')), user_path(book.user.id) %>
            <td>
              <%= link_to user_path(book.user.id) do %>
                <%= image_tag book.user.get_profile_image('80x80') %>
              <% end %>
            </td>
            <td><%= link_to book.title,book_path(book.id) %></td>
            <td><%= book.body %></td>
          </tr>
        <% end %>
      </tbody>
            </table>
        </div>
      </div>
    </diV>
